# Dotfiles
A collection of dotfiles.

# Usage
## Download
```
git clone --depth 1 (path_to_this_repol)
```

## Enable features
`GNU stow` is useful.

e.g. To enable dotfiles for vim:
```
cd dotfiles
stow -v vim
```
